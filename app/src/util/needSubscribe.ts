// Wellspring: all features are free — subscription gates removed.
// These functions are kept as stubs so callers don't need changing.

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const needSubscribe = (_tip?: string) => {
    return false;
};

export const isPaidUser = () => {
    return true;
};
